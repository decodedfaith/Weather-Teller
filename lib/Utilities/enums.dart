enum BottomSheetType {
  notification,
  forcastReport,
}

enum ConnectivityStatus {
  wifi,
  mobile,
  online,
  offline,
}

/// The type of snackbar to show
enum SnackbarType {
  success,
  failure,
}